from pathlib import Path

verbosity = 0
path = f"{Path.home()}/Pictures/Wallpapers"  # picks an image from the user personal picture folder
timer = 0  # timer for slideshow mode, 0 to disable slideshow
# set both of these to 'ask' to have a prompt asking to fill these every time you run it
